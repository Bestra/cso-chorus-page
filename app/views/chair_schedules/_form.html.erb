<%= form_for(@chair_schedule) do |f| %>
  <% if @chair_schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chair_schedule.errors.count, "error") %> prohibited this chair_schedule from being saved:</h2>

      <ul>
      <% @chair_schedule.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :member_id %><br />
    <%= f.number_field :member_id %>
  </div>
  <div class="field">
    <%= f.label :schedule_date %><br />
    <%= f.datetime_select :schedule_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
